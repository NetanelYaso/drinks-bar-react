import React    from "react";
import template from "./Registration.spec.js";

class Registration extends React.Component {
  render() {
    return template.call(this);
  }
}

export default Registration;
